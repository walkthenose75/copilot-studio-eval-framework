# Auto-detect text files and normalize to LF in the repository.
* text=auto

# Shell scripts must use LF so they run on Linux / CI.
*.sh   text eol=lf

# Windows-native scripts use CRLF on checkout.
*.ps1  text eol=crlf
*.bat  text eol=crlf
*.cmd  text eol=crlf
